# Set to true to use lowercase keywords instead of uppercase.
lowercaseKeywords: false
connections:
  - alias: ips_gaaaa
    driver: postgresql
    proto: tcp
    user: alvaro9rqc
    passwd: contraseña
    host: 127.0.0.1
    port: 5432
    dbName: ips_1
