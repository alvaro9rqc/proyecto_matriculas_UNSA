// Code generated by goa v3.21.0, DO NOT EDIT.
//
// Auth service
//
// Command:
// $ goa gen github.com/enrollment/design/api

package auth

import (
	"context"
)

// Service to handle authentication and authorization
type Service interface {
	// Returns information about the currently authenticated user.
	Me(context.Context) (res *AccountUser, err error)
	// Initiates the Google OAuth 2.0 login flow by redirecting to the Google
	// authorization endpoint.
	GoogleLogin(context.Context) (err error)
	// Logs the user out by clearing the session and redirecting to the appropriate
	// post-logout URL.
	GoogleLogout(context.Context) (err error)
	// Google OAuth callback. Creates or reuses a session and redirects to the
	// frontend.
	GoogleCallback(context.Context) (err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "course"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "Auth"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [4]string{"Me", "Google login", "Google logout", "GoogleCallback"}

// AccountUser is the result type of the Auth service Me method.
type AccountUser struct {
	// Unique user ID
	ID string
	// User email
	Email string
	// User first name
	FirstName string
	// User last names
	LastNames string
	// User remaining names
	RemainingNames string
	// User attendee group name
	AttendeeGroupName string
	// User attendee group priority
	AttendeeGroupPriority string
}
